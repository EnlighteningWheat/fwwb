// 这里假设knowledge_graph.js中有一个名为getAnswer的函数，用于根据用户问题从知识图谱中获取答案
async function getAnswer(question) {
    // 这里实现根据用户问题从知识图谱中获取答案的逻辑
    // 返回一个包含答案的Promise对象
    return "这是从知识图谱中获取的答案";
}

module.exports = { getAnswer };